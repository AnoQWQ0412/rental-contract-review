// ============================================================
// tools.js —— 领域工具（纯客户端版）
// 与 Python 版 tools.py 对应，但技能规则直接内嵌，APK 自包含
// ============================================================

// ---- 技能规则库（对应 Python 版的 skills/*.md，内嵌实现） ----
const SKILLS = {
  "通用": `# 租赁通用审查规程
适用于各类租赁，先检查通用风险，再按类型加载专属规程。

## 主体审查
1. 出租方/承租方名称是否明确（自然人或法人）
2. 法人租赁是否写明了统一社会信用代码

## 租赁标的
3. 租赁物描述是否明确（位置/型号/数量/规格）
4. 标的权属是否由出租方享有（表述不清是高风险）

## 租期
5. 起止日期是否明确，是否写明时长
6. 未约定或约定不明视为不定期租赁（高风险）

## 租金
7. 金额是否明确（单位、单价、总价）
8. 支付方式/周期/截止日是否明确
9. 租金调整机制是否约定

## 押金
10. 押金金额是否明确
11. 押金是否超过合理范围（一般1-2倍，特殊类型按专属规程）
12. 押金退还条件/期限/是否计息是否明确

## 费用与维修
13. 物业费/水电费等费用分摊是否明确
14. 维修责任划分是否明确（自然损耗 vs 人为损坏）

## 使用与限制
15. 转租/转借是否明确约定
16. 用途限制是否明确

## 违约责任
17. 提前解除的违约金是否约定，是否对等
18. 违约金是否过高

## 解除与争议
19. 解除条件是否明确
20. 争议解决方式是否明确

## 落款
21. 签署日期是否明确
22. 一式几份是否写明

审查要求：逐条对照，未约定的如实记录为"未约定"而不是"无风险"。`,

  "住宅": `# 住宅租赁专属审查规程
1. 押金一般不超过1-2个月租金；超过2个月标注高风险
2. 居住人数限制是否明确
3. 是否约定不得改变房屋结构或用途
4. 是否可以饲养宠物（未约定标待复核）
5. 家具家电清单是否附有（缺损登记）
6. 交房验收时水表电表燃气表底数是否记录
7. 主体结构/原有设施自然老化维修由出租方负责
8. 是否约定需办理租赁登记备案`,

  "商铺": `# 商铺/商业租赁专属审查规程
1. 押金一般1-3个月租金；超过3个月需谨慎
2. 经营用途是否明确
3. 是否约定出租方协助办理营业执照、消防审批
4. 商铺权属/性质是否适合经营（产权性质不符是高风险）
5. 装修投入归属与租期终止后的处理是否约定
6. 交还时是否要求恢复原状
7. 租期届满后是否有优先承租权约定
8. 物业/管理费、公共区域使用是否明确
9. 长期停业是否导致解除（对经营方是风险）`,

  "办公": `# 办公/写字楼租赁专属审查规程
1. 押金一般1-2个月租金
2. 建筑面积/工位数/配套车位是否明确
3. 办公时间/进出管理是否约定
4. 中央空调/物业费（单价与计算方式）是否明确
5. 交付标准（毛坯/精装）与免租装修期是否约定
6. 是否明确开具增值税发票及税率
7. 工位/区域转租或共享办公是否被禁止（约定不明确是风险）`,

  "设备": `# 设备/机械租赁专属审查规程
1. 押金一般不超过设备价值的10%-20%
2. 设备型号/序列号/新旧程度/附件清单是否明确
3. 数量/单位是否明确
4. 交付地点/方式/时间是否明确
5. 验收标准与异议期限是否约定
6. 维修保养责任划分是否明确（日常维护 vs 大修）
7. 耗材/配件由谁承担是否约定
8. 损毁/灭失责任与赔偿标准是否约定
9. 是否要求投保及保险受益人归属
10. 计费方式（按日/月/台班）与结算周期是否明确`,

  "车辆": `# 车辆租赁专属审查规程
1. 押金一般不超过月租金或车辆价值的10%-20%
2. 违章押金/保证金是否单独约定
3. 车牌号/车型/车架号/行驶证信息是否明确
4. 车辆保险情况与事故责任划分是否明确
5. 行驶里程限制是否约定
6. 是否限制跨省/跨区域行驶
7. 使用用途限制（营运 vs 非营运）是否明确
8. 违章责任与处理流程是否约定
9. 事故处理责任划分、维修/停运损失承担是否明确
10. 保养责任与费用承担是否明确`,

  "其他": `# 其他/未知类型租赁
1. 尝试判断该租赁属于什么类型；无法判断则如实说明
2. 重点核对通用风险：主体、标的、租期、租金、押金、费用、维修、违约、解除
3. 押金合理性：超过2-3倍需谨慎
4. 标的物描述是否足够明确
5. 无法确定类型时，document_type="其他"，仍按通用规则审查
6. 如果输入根本不是租赁合同（如借款、买卖、劳动协议），document_type="非租赁"，并说明更像什么合同`,

  "借条": `# 借条/非租赁判断
当输入明显是借款、买卖、劳动等非租赁合同时使用。
1. document_type="非租赁"
2. risks 可为空
3. summary 中说明这更可能是什么类型的合同
4. 不强行套用租赁规则`,
};

// ---- 工具定义（给模型的说明书，与 Python 版 TOOLS 对应） ----
const TOOLS = [
  {
    type: "function",
    function: {
      name: "load_skill",
      description: "按需加载一份租赁审查规程（规则清单）。审查前先识别租赁类型，加载对应规程；必须加载『通用』+ 该类型一份。",
      parameters: {
        type: "object",
        properties: {
          skill: { type: "string", enum: Object.keys(SKILLS), description: "要加载的规程名称" },
        },
        required: ["skill"],
      },
    },
  },
  {
    type: "function",
    function: {
      name: "lookup_regulation",
      description: "查询法律/法规中关于某个条款的规定要点。审查需要引用法规依据时使用。",
      parameters: {
        type: "object",
        properties: {
          topic: { type: "string", description: "法规主题，如 押金、租赁期限、违约金上限" },
        },
        required: ["topic"],
      },
    },
  },
  {
    type: "function",
    function: {
      name: "search_clause",
      description: "在已提供的合同中检索包含指定关键词的条款原文。需要准确定位某条款原文时使用（合同全文已在上方，通常无需调用）。",
      parameters: {
        type: "object",
        properties: {
          keyword: { type: "string", description: "检索关键词，如 押金、违约金、租期" },
        },
        required: ["keyword"],
      },
    },
  },
];

// ---- 工具实现（与 Python 版 tools.py 的 TOOL_IMPL 对应） ----
const TOOL_IMPL = {
  load_skill(skill) {
    return SKILLS[skill] || `没有『${skill}』这份规程，可选：${Object.keys(SKILLS).join("/")}`;
  },
  lookup_regulation(topic) {
    const REGULATIONS = {
      "押金": "押金金额一般不超过月租金的1-2倍（各地规定不同）；收取押金应开具收据；租赁期满且无违约应无息退还。押金明显高于1-2倍的条款存在风险。",
      "租赁期限": "租赁期限应明确起止日期；未约定或约定不明视为不定期租赁，双方可随时解除合同，对承租方不利。",
      "违约金": "违约金过高可请求法院酌减，以实际损失为基准；约定过高的违约金条款存在被酌减的风险。",
    };
    const key = Object.keys(REGULATIONS).find(k => topic.includes(k));
    return key ? REGULATIONS[key] : `暂无『${topic}』相关法规要点，请人工确认`;
  },
  search_clause(keyword) {
    // 需要访问当前合同文本（由全局 window.__contract 提供，审查时设置）
    const text = window.__contract || "";
    const sentences = text.split(/[。；\n]/).map(s => s.trim()).filter(Boolean);
    const hits = sentences.filter(s => s.includes(keyword));
    return hits.length ? hits.map(h => `- ${h}`).join("\n") : `合同中未找到包含『${keyword}』的条款`;
  },
};

// 执行一个工具调用，返回结果字符串（与 Python 版 agent.py 对应）
function executeTool(name, args) {
  const fn = TOOL_IMPL[name];
  if (!fn) return `未知工具: ${name}`;
  try {
    return String(fn(...Object.values(args)));
  } catch (e) {
    return `工具执行异常: ${e.message}`;
  }
}
